# Model Weights

Place exported PyTorch weights here. Expected default path is `models/cough_detector.pth`
as configured in `app/core/config.py`.

Suggested layout:

```
models/
└── cough_detector.pth
```

Update the `BACKEND_MODEL_WEIGHTS_PATH` environment variable if you use a different file.

